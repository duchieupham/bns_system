import 'package:flutter/material.dart';

class BNSWebApp extends StatelessWidget {
  const BNSWebApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Container(),
    );
  }
}
